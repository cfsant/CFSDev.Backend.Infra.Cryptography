name: CFSDev.Backend.Infra.Cryptography

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      NAME: CFSant.Dev.Commons
      VERSION: 1.0.0

    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 6.0.x
      - name: Restore dependencies
        run: dotnet restore ./Backend.Infra.Cryptography.csproj
      - name: Build
        run: dotnet build --no-restore ./Backend.Infra.Cryptography.csproj
      - name: Pack
        run: dotnet pack -o ./ ./Backend.Infra.Cryptography.csproj -p:IncludeSymbols=true -p:SymbolPackageFormat=snupkg
      - name: Publish
        run: nuget push ./${{env.NAME}}.${{env.VERSION}}.nupkg -Source 'https://api.nuget.org/v3/index.json' -ApiKey ${{secrets.MAIN}} -SkipDuplicate
